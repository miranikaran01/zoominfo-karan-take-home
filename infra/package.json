{
    "name": "karan-take-home-infra",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "build": "tsc",
      "synth": "npm run build && npx aws-cdk synth",
      "deploy": "npm run build && npx aws-cdk deploy --require-approval never",
      "diff": "npm run build && npx aws-cdk diff"
    },
    "dependencies": {
      "aws-cdk-lib": "^2.38.0",
      "constructs": "^10.0.0"
    },
    "peerDependencies": {
      "aws-cdk-lib": "^2.38.0",
      "constructs": "^10.0.0"
    },
    "devDependencies": {
      "@types/node": "22.13.10",
      "typescript": "5.8.2"
    }
}  